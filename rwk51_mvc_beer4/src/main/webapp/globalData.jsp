<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%!
	//static final String rootDir = "/rwk51_mvc_beer"; //root다이렉토리 경로를 저장한다.
	static final String rootDir = "/rwk51_mvc_beer4"; //DBCP 경로 다이렉토리로 지정
	static final String workDir = rootDir +"/com/yju/2wda/team2"; //우리가 파일을 저장해준 경로를 지정한다.
	
	static final String modelDir = workDir +"/model"; //workDir안에 있는 model 경로에 접속
	static final String viewDir = workDir + "/view"; //workDir안에 있는 view 경로에 접속
	static final String contDir = workDir +"/controller"; //workDir안에 있는 controller에 접속
	
	static final String beerModelDir = modelDir + "/beer"; //modelDir경로의 beer에 접속
	static final String beerViewDir = viewDir + "/beer"; //view경로의 beer 에 접속
	static final String beerContDir = contDir + "/beer"; //contDir 경로의 beer에 접속 
	
	static final String etcViewDir = viewDir + "/etc"; //viewDir에 기타에 접속
	
	static final String imgDir = rootDir + "/image";

%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

</body>
</html>