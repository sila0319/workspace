<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
    
<%!
	
	static final String rootDir = "/rwk51_boardgame_mall"; //DBCP 경로 다이렉토리로 지정
	static final String workDir = rootDir +"/com/yju/2wda/ryu"; //우리가 파일을 저장해준 경로를 지정한다.
	
	static final String modelDir = workDir +"/model"; //workDir안에 있는 model 경로에 접속
	static final String viewDir = workDir + "/view"; //workDir안에 있는 view 경로에 접속
	static final String contDir = workDir +"/controller"; //workDir안에 있는 controller에 접속
	
	static final String userModelDir = modelDir + "/user"; //modelDir경로의 beer에 접속
	static final String userViewDir = viewDir + "/user"; //view경로의 user 에 접속
	
	static final String boardgameModelDir = modelDir + "/boardgame"; //boardgameview model 경로설정
	static final String boardgameViewDir = viewDir +"/boardgame"; //boardgameview 경로 설정
	
	static final String etcViewDir = viewDir + "/etc"; //viewDir에 기타에 접속
	
	static final String imgDir = rootDir + "/image"; //이미지경로
	static final String oriDir = imgDir + "/original"; //원본이미지
	static final String thuDir = imgDir + "/thumb"; //썸네일이미지
	
	static final String CSSDir = rootDir +"/CSS"; //CSS 경로
	
	static final String jsDir = rootDir +"/JS"; //자바스크립트 경로
	static final String jsUserDir = jsDir +"/user"; //자바스크립트 안의 user 폴더의 경로
	static final String jsBoardgameDir = jsDir +"/boardgame"; //boardgmae 폴더의 경로
	

%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

</body>
</html>